/** Islands run on the same origin as Laravel, so the API is simply /api/. */
const API_URL = '/api/';
const API_ORIGIN = '/';

const config = {
  API_URL,
  API_ORIGIN,
  LANDING_HERO_VARIANT: 'default',
  WALKTHROUGH_VIDEO_URL: '',
  SHOW_WALKTHROUGH_SECTION: false,
};

export default config;
